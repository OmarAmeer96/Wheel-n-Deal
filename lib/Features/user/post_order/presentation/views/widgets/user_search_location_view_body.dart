import 'package:flutter/material.dart';

class UserSearchLocationViewBody extends StatelessWidget {
  const UserSearchLocationViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const SafeArea(
      child: Placeholder(),
    );
  }
}
