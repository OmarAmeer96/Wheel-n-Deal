import 'package:flutter/widgets.dart';

class UserCompletedOrdersViewBody extends StatelessWidget {
  const UserCompletedOrdersViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
